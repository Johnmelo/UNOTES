<p id="notice"><%= notice %></p>

<p>
  <b>Email:</b>
  <%= @user.email %>
</p>
<p>
	<b>Grupos:</b>
	<% Group.find (:all) do |t| %>
		<% if t.user_ids == current_user.id %>
			<%= t.name %>
		<% end %>	
	<% end %>
</p>


<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>